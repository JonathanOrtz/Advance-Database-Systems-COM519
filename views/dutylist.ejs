<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta http-equiv="X-UA-Compatible" content="IE=edge" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-1BmE4kWBq78iYhFldvKuhfTAU6auU8tT94WrHftjDbrCEXSU1oBoqyl2QvZ6jIW3" crossorigin="anonymous">
    <title>Duties LIST</title>
  </head>
  <body>
    <header><%- include('common/header'); -%></header>
    <table class="table">
        <thead>
          <tr>
            <th scope="col">Number</th>
            <th scope="col">Title</th>
            <th scope="col">detailing</th>
            <th scope="col">status</th>
          </tr>
        </thead>
        <tbody> 
          <% for(let i=0; i< dutieslist.length; i++){ %>
          <tr>
            <td><%= i+1 %></td>
            <td><%=dutieslist[i].title %></td>
            <td><%=dutieslist[i].detailing %></td>
            <td>
                <a class="btn btn-primary" href="/update/<%= dutieslist[i]._id %>" role="button">UPDATE</a>
                <a class="btn btn-primary" href="/delete/<%= dutieslist[i]._id %>" role="button">DELETE</a>


                <% if(dutieslist[i].done) { %>
                <a class="btn btn-primary" href="/status/<%= dutieslist[i]._id %>" role="button">DONE</a>
                <% } else { %>
                <a class="btn btn-primary" href="/status/<%= dutieslist[i]._id %>" role="button">NOT DONE</a>
                <% } %>
                


            </td>
          </tr>
          <% } %>
        </tbody>
      </table>
  </body>
</html>
